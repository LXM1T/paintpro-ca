# ============================================================
# PaintPro CA — Variables de Entorno
# ⚠️  NUNCA subir este archivo a GitHub
# ⚠️  Está incluido en .gitignore
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
# Encontrar en: supabase.com → Tu proyecto → Settings → API

# URL del proyecto (pública, segura de usar en frontend)
SUPABASE_URL=https://yqctbsyykxxojueciqam.supabase.co

# Clave anon/public (pública, va en el frontend)
# Permisos limitados por RLS — segura de exponer
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlxY3Ric3l5a3h4b2p1ZWNpcWFtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODAyNzc0OTgsImV4cCI6MjA5NTg1MzQ5OH0.QsznISsDdcZdoLabcMXC4cyWCNBLDrbK_qJ5k1aUQfg

# Clave service_role (PRIVADA — solo para backend/migraciones)
# ⛔ NUNCA poner en código frontend o repositorio público
# SUPABASE_SERVICE_ROLE_KEY=eyJ...  ← descomenta solo en servidor

# ── App ───────────────────────────────────────────────────────
APP_URL=https://paintpro-ca.vercel.app
APP_NAME=PaintPro CA
APP_ENV=production

# ── Vercel (si usas Vercel CLI localmente) ───────────────────
# VERCEL_TOKEN=...

# ============================================================
# INSTRUCCIONES PARA EL EQUIPO
# ============================================================
# 1. Copia este archivo como .env en tu máquina local
# 2. Rellena los valores que correspondan a tu entorno
# 3. NUNCA hagas commit de .env — está en .gitignore
# 4. Para producción, agrega las variables en:
#    Vercel → Tu proyecto → Settings → Environment Variables
# ============================================================
